{
  "name": "personal-site",
  "version": "0.1.0",
  "homepage": "http://coreyburkhart.com/",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-router": "^3.0.1",
    "scroll-into-view": "^1.7.1"
  },
  "scripts": {
    "watch-sass": "mkdir -p ./src/css && sass --watch src/scss:src/css",
    "start": "node server.js",
    "startDev": "npm run watch-sass & react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
