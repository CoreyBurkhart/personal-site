{
  "name": "personal-site",
  "version": "0.1.0",
  "main" : "server.js",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "express": "^4.14.0",
    "path": "^0.12.7",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-materialize": "^0.17.6"
  },
  "scripts": {
    "watch-sass": "mkdir -p ./src/css && sass --watch src/scss:src/css",
    "start": "npm run watch-sass & react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
